// import {state as company_state} from './state';

export const policies = (state) => {
    return state.policies;
}

export const policy = (state) => {
    return state.policy;
}

export const plan = (state) => {
    return state.plan;
}