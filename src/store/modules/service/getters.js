// import {state as serviceState} from './state';

export const services = (state) => {
    return state.services;
}

export const service = (state) => {
    return state.service;
}

export const service_insurance_details = (state) => {
    return state.service_insurance_details;
}