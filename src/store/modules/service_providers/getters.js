// import {state as serviceState} from './state';

export const serviceProviders = (state) => {
    return state.service_providers;
}

export const serviceProvider = (state) => {
    return state.service_provider;
}

